{
  "name": "plasma-payment-gateway-token",
  "version": "1.0.0",
  "description": "PGT Token - Decentralized Stablecoin Payment Gateway with Bitcoin On-Ramps",
  "scripts": {
    "test": "npx hardhat test",
    "compile": "npx hardhat compile",
    "deploy": "npx hardhat run scripts/deploy.js --network plasmaTestnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.22.1"
  }
}